{
  "name": "agmiex-website",
  "version": "2.0.0",
  "private": true,
  "description": "AGMIEX marketing website — static HTML, CSS and vanilla JS with an optional optimised build.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs --dist",
    "test": "node scripts/check-site.mjs && node --test \"tests/unit/*.test.mjs\"",
    "test:e2e": "python tests/e2e/qa.py"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
